name: keywordia
version: 1.0.0
description: Keywordia application

container:
  image: keywordia:latest
  port: 3000

resources:
  cpu: 500m
  memory: 512Mi

replicas: 1

env:
  - name: NODE_ENV
    value: production

